---
# Skeleton generated by tools/deck_skeleton.py — author live during the review window.
# Headings are the public interface (run-sheets cite them): rename the TODO-rename ones
# to this unit's own words, then update lectures/unit02-runsheet.md to match.
title: "Repeat: null distributions and the thousand-test trap"
subtitle: "BF550 · Unit 2 · First meeting — 2026-09-11"
---

## Under construction {.center}

<!-- banner:under-construction -->

⚠️ This deck is a **draft** — not yet instructor-reviewed, subject to change until the
session it serves.

# TODO-rename: the unit's opening question

## Check-in {.smaller}

TODO: the check-in link and its QR code — `?session=NN`

::: {.notes}
TODO: the standing frame, verbatim; glance at the split before moving on.
Opener kind; bank `checkins/unit02.md` session 1.
:::

## TODO-rename: activation beat 1 {.smaller}

Target misconception: *"A small p-value means the effect is real."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 2 {.smaller}

Target misconception: *"If each test is honest, the list of significant results is honest."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 3 {.smaller}

Target misconception: *"1,200 tests are 1,200 independent questions."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## PS2 opens today

On **unit 1's material** — the PTC problem from unit 1, with our design in the notebook.
Title and one teaser sentence ONLY; the notebook carries the problem and our design of it.

Graded on the checks passing and on your answers.

::: {.notes}
The first graded set. PS1 is not collected (instructor, decision 17 of the overhaul record);
that is said in the room if asked and never put on a slide. What the room hears about the change
of shape is bf550-instructor#41. Never the planted truth of PS2's data or its key.
:::

::: {.notes}
TODO: the teaser sentence. Never the planted truth of the set's data on this slide
(bf550-instructor authoring/assignments.md, `truth-is-planted`). See bf550-instructor#41.
:::

## Working time — the rest of today

[Lab: unit-02-1](https://bu-cds-bf550.github.io/labs/unit-02-1/)

::: {.notes}
TODO: today's priorities, in order; what gets cut first if the clock slips.
:::

## Before next session

- TODO: the reading / the due dates, from the schedule
- PS2 is open

::: {.notes}
TODO: say the due dates out loud; they are also on every set header.
:::
