---
# Skeleton generated by tools/deck_skeleton.py --unit 4 --session 3; author live
# during the review window. Headings are the public interface (run-sheets cite them, and
# they cite deck + session): rename the TODO-rename ones to this unit's own words, then
# update lectures/unit04-runsheet.md to match.
title: "Mix: which process produced this observation?"
subtitle: "BF550 · Unit 4 · Session 3 — Wed Sep 30"
---

## Under construction {.center}

<!-- banner:under-construction -->

⚠️ This deck is a **draft** — not yet instructor-reviewed, subject to change until the
session it serves.

# TODO-rename: session 3's beat

## Check-in {.smaller}

TODO: the check-in link and its QR code — `?session=NN`

::: {.notes}
TODO: the standing frame, verbatim; glance at the split before moving on.
Code-reading kind; bank `checkins/unit04/bank.md` session 3.
:::

## Where we left it

- TODO: one slide of callback, the claim session 2 landed
- TODO: the beat this session adds. **Activation, not delivery**: a deck that
  re-teaches the chapter is too long, and a later session's deck is shorter still.

::: {.notes}
TODO: session 3 is mostly working time. Name here what gets cut if the clock slips.
:::

## TODO-rename: activation beat 1 {.smaller}

Target misconception: *"A confident posterior means the read strongly resembles organism A."*

**Predict first.** Collect predictions out loud, then run the seeded chunk with the chapter's seed.

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat: chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to listen for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 2 {.smaller}

Target misconception: *"A classifier trained on a community works in any community the organisms appear in."*

**Predict first.** Collect predictions out loud, then run the seeded chunk with the chapter's seed.

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat: chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to listen for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 3 {.smaller}

Target misconception: *"A probability estimated as zero means the data showed something is impossible."*

**Predict first.** Collect predictions out loud, then run the seeded chunk with the chapter's seed.

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat: chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to listen for, and the sentence that lands the correction.
:::

## Working time — the rest of today

[Lab: unit-04-1](https://bu-cds-bf550.github.io/labs/unit-04-1/)

::: {.notes}
TODO: today's priorities, in order; what gets cut first if the clock slips.
:::

## Before next session

- TODO: the reading and the due dates, from the schedule
- PS— is open

::: {.notes}
TODO: say the due dates out loud; they are also on every set header.
:::
