---
# Skeleton generated by tools/deck_skeleton.py --unit 5 --session 2 — author live
# during the review window. Headings are the public interface (run-sheets cite them, and
# they cite deck + session): rename the TODO-rename ones to this unit's own words, then
# update lectures/unit05-runsheet.md to match.
title: "Naive Bayes: classification as a generative story"
subtitle: "BF550 · Unit 5 · Session 2 — Mon Oct 5"
---

## Under construction {.center}

<!-- banner:under-construction -->

⚠️ This deck is a **draft** — not yet instructor-reviewed, subject to change until the
session it serves.

# TODO-rename: session 2's beat

## Check-in {.smaller}

TODO: the check-in link and its QR code — `?session=NN`

::: {.notes}
TODO: the standing frame, verbatim; glance at the split before moving on.
Reading kind; bank `checkins/unit05/bank.md` session 2.
:::

## Where we left it

- TODO: one slide of callback — the claim session 1 landed
- TODO: the beat this session adds. **Activation, not delivery**: a deck that
  re-teaches the chapter is too long, and a later session's deck is shorter still.

::: {.notes}
TODO: session 2 is mostly working time. Name here what gets cut if the clock slips.
:::

## TODO-rename: activation beat 1 {.smaller}

Target misconception: *"A posterior is a fancier p-value."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 2 {.smaller}

Target misconception: *"A posterior of 0.99 means the classifier is right 99% of the time on calls like this one."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## TODO-rename: activation beat 3 {.smaller}

Target misconception: *"Underflow is a rare numerical edge case, not a modeling concern."*

**Predict first** — collect out loud — then run the seeded chunk (reuse the chapter's seed).

```{python}
#| echo: true
# TODO: the predict-then-run cycle for this beat — chapter chunk, chapter seed
```

::: {.notes}
TODO: the wrong answers to fish for, and the sentence that lands the correction.
:::

## Working time — the rest of today

[Lab: unit-05-1](https://bu-cds-bf550.github.io/labs/unit-05-1/)

::: {.notes}
TODO: today's priorities, in order; what gets cut first if the clock slips.
:::

## Before next session

- TODO: the reading / the due dates, from the schedule
- PS— is open

::: {.notes}
TODO: say the due dates out loud; they are also on every set header.
:::
